after a pip install:

pip freeze > requirements.txt
git add ./requirements.txt


activating the python venv:

source .venv/bin/activate


if python libraries are missing:

pip install -r requirements.txt